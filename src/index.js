import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Takes whatever is in app and sends it to the /public/index.html
ReactDOM.render(<App />, document.getElementById('root'));

